<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme; Compose drives the actual UI colors. NoActionBar + edge-to-edge friendly. -->
    <style name="Theme.ExpenseTracker" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/cloud</item>
    </style>
</resources>
